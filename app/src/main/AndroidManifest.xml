<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aluno.daybook">

    <aplication
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">


    </aplication>

    <application>
        <activity
            android:name=".TelaLogin"
            android:label="@string/title_activity_tela_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdicionarLembrete"></activity>
        <activity
            android:name=".TelaInicial"
            android:label="@string/title_activity_tela_inicial"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".TelaCadastro" />
        <activity android:name=".TelaPrincipal"/>
        <activity android:name=".AdicionarItem" />
        <activity android:name=".AdicionarEvento" />
        <activity android:name=".AdicionarMateria" />
        <activity android:name=".Configuracoes" />
        <activity android:name=".VerEventos" />
        <activity android:name=".VerLembretes" />
        <activity android:name=".VerMaterias" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AlteraLembretes" />
    </application>

</manifest>